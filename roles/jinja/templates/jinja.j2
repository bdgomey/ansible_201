{% if {{ item.interface }} != VLAN2 %}
 {% for item in ip_interfaces %}
 interface {{ item.interface }}
  description {{ item.description }}
  no shutdown
  no switchport
  ip address {{ item.ip }} 255.255.255.0
 exit
 {% endfor %}
{% else %}
 {% for item in ip_interfaces %}
 interface {{ item.interface }}
  description {{ item.description }}
  no shutdown
  ip address {{ item.ip }} 255.255.255.0
 exit
 {% endfor %}
{% endif %}